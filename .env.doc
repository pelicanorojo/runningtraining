#This file is used by prisma and docker compose
DATABASE_URL="postgresql://postgres@xxxxxx:nnnn/postgres?schema=public"

# PRISMA TODO: dedicated schema

DATABASE_URL="postgresql://[POSTGRES_MIGRATION_USER]:[POSTGRES_MIGRATION_PASSWORD]@localhost:5432/[APP_DB_NAME]?schema=public"

# DOCKER COMPOSE

# Postgre DB
APP_DB_NAME=[marathon_db]
#DB_DATA_PATH=[FOLDERFORPGDB]
COMPOSE_BASE=[COMPOSEAUXFOLDER]
POSTGRE_DATA=[FOLDERFORPGDB]

# Admin (for user creation only, not used by app)
POSTGRES_ADMIN_USER=pgadmin
POSTGRES_ADMIN_PASSWORD=[pgadminpassword]

# Migration user (elevated but not full admin)
POSTGRES_MIGRATION_USER=migrator
POSTGRES_MIGRATION_PASSWORD=[migratorpassword]

# App user (limited permissions)
POSTGRES_APP_USER=Kipchogue
POSTGRES_APP_PASSWORD=[appuserpassword]
